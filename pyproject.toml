[project]
name = "hiviper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hiviper = "hiviper.viper_ascii:cli"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"